

<div>
<a class="btn btn-accent-inverse" role="button" data-toggle="collapse" href="#collapseExample_{{group.id}}" aria-expanded="false" aria-controls="collapseExample">
  {{group.title}}
</a>
	<div class="collapse" id="collapseExample_{{group.id}}">
  		<div class="well">

  {{#each group.suites as |suite|}}
  {{> suite_select suite=suite}}
  {{/each}}

      </div>
	</div>
</div>

